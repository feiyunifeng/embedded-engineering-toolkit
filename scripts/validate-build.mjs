import{readdir,readFile,stat}from'node:fs/promises';import{join,relative}from'node:path';const root=new URL('../dist/',import.meta.url).pathname.replace(/^\/(.:)/,'$1');async function files(d){return(await Promise.all((await readdir(d)).map(async n=>{const p=join(d,n);return(await stat(p)).isDirectory()?files(p):p}))).flat()}const all=await files(root),html=all.filter(x=>x.endsWith('.html'));if(html.length!==14)throw Error(`Expected 14 HTML pages, found ${html.length}`);const routes=new Set(html.map(x=>{const r=relative(root,x).replaceAll('\\','/');return r==='index.html'?'/':'/'+r.replace(/index\.html$/,'')}));const broken=[];for(const f of html){const s=await readFile(f,'utf8');for(const m of s.matchAll(/href="(\/[^"]*)"/g)){const p=m[1].split(/[?#]/)[0];if(p.endsWith('/')&&!routes.has(p))broken.push(`${relative(root,f)} -> ${p}`)}}if(broken.length)throw Error(`Broken internal links:\n${broken.join('\n')}`);for(const name of ['robots.txt','sitemap-index.xml','favicon.svg','site.webmanifest'])if(!all.some(x=>x.endsWith(name)))throw Error(`Missing ${name}`);const text=(await Promise.all(all.filter(x=>/\.(html|js|css|txt)$/.test(x)).map(x=>readFile(x,'utf8')))).join('\n');for(const bad of ['pub-000000','Lorem ipsum','Coming Soon'])if(text.includes(bad))throw Error(`Forbidden placeholder: ${bad}`);console.log(`Validated ${html.length} pages and ${all.length} build files; internal links are intact.`);
