---
import Article from '../../../components/ZhArticlePage.astro';
---
<Article title="如何估算 CAN 总线负载" description="了解 Classical CAN 的开销、位填充、标识符格式和总线负载估算限制。" eyebrow="Classical CAN 指南" enPath="/guides/can-bus-load/"><h2>容量由完整帧消耗</h2><p>CAN 流量包含仲裁、控制、CRC、确认、分隔符、数据和帧间隔。本工具对 11 位标准标识符使用 47 个基准位，对 29 位扩展标识符使用 67 个基准位，再加数据位。</p><div class="formula">负载 % = 帧位数 × 帧/秒 ÷ 比特率 × 100</div><h2>Worked Example：500 kbit/s Classical CAN</h2><p>场景：500 kbit/s、11 位标准标识符、8 字节数据、每秒 1000 帧。以下帧长度包含最小 3-bit intermission，是理论容量估算。</p><ol><li>无填充帧 = 47 + 8 × 8 = <strong>111 bit</strong>。</li><li>帧时间 = 111 ÷ 500000 = <strong>222 μs</strong>。</li><li>无填充负载 = 111 × 1000 ÷ 500000 × 100 = <strong>22.2%</strong>。</li><li>保守估算 = ceil(111 × 1.20) = <strong>134 bit</strong>；帧时间 268 μs，负载 <strong>26.8%</strong>。</li></ol><p>20% 是本工具的规划余量，不是协议规定的严格最坏上界；实际填充取决于位模式，且未计入重传、错误帧、仲裁延迟、过载和其他流量。此处仅使用 Classical CAN，不使用 CAN FD 公式。参考 <a href="https://tekeye.uk/downloads/can2spec.pdf" rel="noopener">Bosch CAN Specification 2.0</a> 和 <a href="https://www.can-cia.org/can-knowledge/can-cc" rel="noopener">CiA CAN CC 概览</a>。</p><p><a class="button" href="/zh/tools/can-bus-load/">打开 CAN 计算器</a></p></Article>
