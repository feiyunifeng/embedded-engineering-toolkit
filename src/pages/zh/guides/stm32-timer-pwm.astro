---
import Article from '../../../components/ZhArticlePage.astro';
---
<Article title="STM32 定时器时钟与 PWM 公式" description="了解 STM32 定时器时钟以及 PSC、ARR、CCR 如何决定 PWM 频率和占空比。" eyebrow="STM32 指南" enPath="/guides/stm32-timer-pwm/"><h2>先确定有效定时器时钟</h2><p>定时器输入来自芯片时钟树。在一些 STM32 系列中，APB 分频不为 1 时定时器可能得到倍频时钟，请以具体芯片文档为准。</p><div class="formula">计数器时钟 = 定时器时钟 ÷ (PSC + 1)<br>PWM 频率 = 定时器时钟 ÷ ((PSC + 1) × (ARR + 1))</div><h2>Worked Example：1 kHz、25% 边沿对齐 PWM</h2><p>输入为 <strong>72 MHz 定时器时钟</strong>，PSC=71、ARR=999、CCR=250；假设向上计数、边沿对齐、PWM 模式 1、有效高极性。这是理论计算，不是硬件实测；72 MHz 不能直接当作 CPU 或 APB 时钟。</p><ol><li>计数器时钟 = 72,000,000 ÷ (71+1) = <strong>1 MHz</strong>。</li><li>周期计数 = 999+1 = <strong>1000</strong>。</li><li>PWM 频率 = 1,000,000 ÷ 1000 = <strong>1 kHz</strong>，周期 1 ms。</li><li>占空比 = 250 ÷ 1000 × 100 = <strong>25%</strong>。</li></ol><p>PWM 模式 1 向上计数时，CNT 小于 CCR 期间输出参考信号为高。中心对齐、向下计数、重复计数器、极性反相、死区和更新时序可能不适用同一公式。参考 STMicroelectronics <a href="https://www.st.com/resource/en/reference_manual/rm0008-stm32f103xx-advanced-armbased-32bit-mcus-stmicroelectronics.pdf" rel="noopener">RM0008 参考手册</a>。</p><p><a class="button" href="/zh/tools/stm32-timer-pwm/">打开 STM32 计算器</a></p></Article>
